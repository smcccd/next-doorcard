# =============================================================================
# PRODUCTION Environment Configuration
# =============================================================================
# This file contains production-specific settings
# Used when NODE_ENV=production (during build and in production)
#
# IMPORTANT: Do NOT commit actual production secrets to git
# Set these values in your hosting platform (Vercel, AWS, etc.)
# =============================================================================

# Database - Production PostgreSQL
# Set this in Vercel/hosting platform environment variables
# DATABASE_URL="postgresql://user:password@host:5432/doorcard?schema=public"

# NextAuth - Production Settings
# Set this in Vercel/hosting platform environment variables
# IMPORTANT: NEXTAUTH_URL must match exactly what's configured in OneLogin redirect URIs
# NEXTAUTH_URL="https://test.doorcard.smccd.edu"
# NEXTAUTH_SECRET="your-production-secret-generate-with-openssl"

# OneLogin PRODUCTION Application
# These should point to your OneLogin PRODUCTION application
# Set these in Vercel/hosting platform environment variables
# ONELOGIN_CLIENT_ID="your-prod-onelogin-client-id"
# ONELOGIN_CLIENT_SECRET="your-prod-onelogin-client-secret"
# ONELOGIN_ISSUER="https://smccd.onelogin.com/oidc/2"

# Sentry - Production Monitoring
# Set these in Vercel/hosting platform environment variables
# SENTRY_DSN="your-production-sentry-dsn"
# NEXT_PUBLIC_SENTRY_DSN="your-production-sentry-dsn"
# SENTRY_AUTH_TOKEN="your-sentry-auth-token"

# Microsoft Clarity - Production Analytics
# Set this in Vercel/hosting platform environment variables
# NEXT_PUBLIC_CLARITY_ID="your-clarity-project-id"

# Logging - Production (errors only)
LOG_LEVEL="error"

# Feature Flags - Production
NEXT_PUBLIC_SHOW_BETA_BANNER="false"
ENABLE_RATE_LIMITING="true"
ENABLE_AUTH_DEBUG="false"

# Fallback Term Configuration
FALLBACK_ACTIVE_TERM_SEASON="FALL"
FALLBACK_ACTIVE_TERM_YEAR="2025"

# Build Configuration
NX_DAEMON="false"
TURBO_CACHE="remote:rw"
TURBO_REMOTE_ONLY="true"
